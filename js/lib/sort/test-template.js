// const { increment, swap } = require('../../utils/utils');

const sort = (arr, config) => {
  const sorted = arr.slice();
  return sorted;
};

module.exports = {
  func: sort,
  oWorst: (n) => n,
  oAvg: (n) => n,
  oBest: (n) => n,
  oSpace: (n) => n,
};
